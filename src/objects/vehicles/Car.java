package objects.vehicles;

import objects.Vehicle;
import objects.VehicleOwner;

public class Car extends Vehicle {

    public Car(VehicleOwner owner, String type) {
        super(owner, type);
    }
}
